import { movies } from "@/app/data"

export default function ActorPage({params}) {
    // const actor = movies.find(x => params.slug.includes(x.map(y=> y.name.split(' ').join('-'))))
  return (
    // <div>{actor.Actors[4].name}</div>
    <p>hi</p>
  )
}
